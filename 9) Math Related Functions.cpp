#include <iostream>
#include <cmath>


int main(){

    double x = 5.56;
    double y = 7;
    double z;

    //z=std::max(x, y); the max value between two values
    //z = std::min (x,y); min between two values
    //z=pow(2,3); 2 to the power 3
    //z=sqrt(9);
    //z = abs(-3);
    //z = round (x);
    //z = ceil (x); rounds up
    //z = floor (x); rounds down


    std::cout << z;

    return 0;
}
